# Service YAML
apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: my-deployment
spec:
  selector:
    app: kubernetes-nodejs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort
